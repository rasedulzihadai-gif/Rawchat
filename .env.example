# Postgres connection string used for saved conversations & messages.
# The app builds and chats without it, but history won't persist until set.
# Example for local Postgres:
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
